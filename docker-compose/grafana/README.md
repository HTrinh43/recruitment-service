# Start grafana

```bash
docker compose up -d
```

# Stop grafana
```bash
docker compose stop
```

# Link console grafana

(http://localhost:3000/)[http://localhost:3000/]

Default login: `admin/admin`